/* Bonfire: Chunky Monkey */

function chunk(arr, size) {
  // Break it up.
  var multi = [];
  var outer;
  var inner;
  var count = 0;
  for (var i = 0; i < arr.length; i++){
  	
  	 

  	
  }
  console.log(count);
  console.log(multi);
  return arr;
}

chunk(['a', 'b', 'c', 'd'], 2);